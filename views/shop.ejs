<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Shop - Dog Store</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">


    <style>
        /* Google Fonts */
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap');

        body {
            font-family: 'Poppins', sans-serif;
            background: #f8f9fa;
            color: #333;

        }

        /* Page Title */
        h2.text-center.my-4 {
            font-weight: 600;
            color: #2c3e50;
        }

        /* Filter Sidebar */
        .form-select,
        .list-unstyled a {
            border-radius: 8px;
            font-size: 15px;
        }

        .list-unstyled a {
            display: block;
            padding: 6px 12px;
            color: #333;
            transition: all 0.2s ease-in-out;
            text-decoration: none;
        }

        .list-unstyled a:hover {
            background-color: #f0f0f0;
            color: #e74c3c;
            border-radius: 6px;
        }

        /* Flash Message */
        .flash-message {
            background: linear-gradient(135deg, #2ecc71, #27ae60);
            color: white;
            padding: 10px 20px;
            border-radius: 8px;
            animation: fadeSlideIn 0.5s ease-out forwards;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            font-weight: 500;
            letter-spacing: 0.5px;
        }

        @keyframes fadeSlideIn {
            from {
                opacity: 0;
                transform: translateY(-15px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Product Cards */
        .card {
            border: none;
            border-radius: 20px;
            overflow: hidden;
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(8px);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-8px);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.1);
        }

        /* Card Image */
        .card-img-top {
            height: 250px;
            object-fit: cover;
            border-radius: 20px 20px 0 0;
        }

        /* Card Body */
        .card-body h5 {
            font-weight: 600;
            color: #2c3e50;
        }

        .card-body p {
            font-size: 14px;
            color: #666;
            margin-bottom: 12px;
        }

        /* Price */
        .card-body h6 {
            color: #e74c3c;
            font-weight: 600;
            font-size: 17px;
        }

        /* Bottom Panel */
        .card .rounded-bottom {
            background: linear-gradient(to right, #3498db, #8e44ad);
            color: white;
            font-size: 14px;
            font-weight: 500;
        }

        /* Add to Cart Button */
        .btn-light.btn-sm {
            background-color: #f7f7f7;
            border-radius: 8px;
            font-weight: 500;
            transition: all 0.3s ease;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        }

        .btn-light.btn-sm:hover {
            background: #2ecc71;
            color: #fff;
            transform: scale(1.1);
        }

        /* Footer */
        footer {
            background: #2c3e50;
            color: #ecf0f1;
            padding: 30px 0;
            text-align: center;
        }

        footer h5 {
            font-weight: 600;
            margin-bottom: 12px;
        }

        footer a {
            color: #ecf0f1;
            margin: 0 8px;
            font-size: 20px;
            background: rgba(255, 255, 255, 0.1);
            padding: 10px;
            border-radius: 50%;
            display: inline-block;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        footer a:hover {
            background: #e74c3c;
            color: white;
        }

        .floating-cart {
    position: fixed;
    bottom: 30px;
    right: 30px;
    background: #e74c3c;
    color: white;
    border: none;
    border-radius: 50%;
    width: 60px;
    height: 60px;
    font-size: 26px;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000;
    box-shadow: 0 6px 14px rgba(0,0,0,0.15);
    transition: transform 0.3s ease;
    text-decoration: none;
}

.floating-cart:hover {
    transform: scale(1.1);
    background: #c0392b;
}

.cart-badge {
    font-size: 13px;
    padding: 5px 8px;
    color: white;
    background: #dc3545;
    border-radius: 50%;
    box-shadow: 0 0 8px rgba(0,0,0,0.2);
}

    </style>
</head>


<a href="/shop/cart" class="floating-cart position-fixed top-0 end-0 m-4 z-3">
    <i class="bi bi-cart fs-3 text-dark"></i>
    <% if (cartCount && cartCount > 0) { %>
        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
            <%= cartCount %>
        </span>
    <% } %>
</a>



<body>

    <% if (success && success.length> 0) { %>
        <div class="container mt-3">
            <div class="flash-message success-flash" id="flash-msg">
                <%= success[0] %>
            </div>
        </div>
        <% } %>





            <header class="p-3 mb-3 border-bottom">
                <div class="container">
                    <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                        <a href="/"
                            class="d-flex align-items-center mb-2 mb-lg-0 link-body-emphasis text-decoration-none">
                            <svg class="bi me-2" width="40" height="32" role="img" aria-label="Bootstrap">
                                <use xlink:href="#bootstrap"></use>
                            </svg>
                        </a>

                        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                            <li><a href="/shop" class="nav-link px-2 link-emphasis">Home</a></li>
                            <li><a href="/shop/cart" class="nav-link px-2 link-body-emphasis">Cart</a></li>
                            <li><a href="#" class="nav-link px-2 link-body-emphasis">Profile</a></li>
                            <li><a href="#" class="nav-link px-2 link-body-emphasis">Support</a></li>
                        </ul>

                        <form class="col-12 col-lg-auto mb-3 mb-lg-0 me-lg-3" role="search">
                            <input type="search" class="form-control" placeholder="Search..." aria-label="Search">
                        </form>


                        <div class="d-flex align-items-center gap-2 mb-3 mb-lg-0 me-lg-3">
                            <a href="/users/logout"> <button type="button" class="btn btn-danger">Logout</button></a>

                            <div class="dropdown text-end">
                                <a href="#" class="d-block link-body-emphasis text-decoration-none dropdown-toggle"
                                    data-bs-toggle="dropdown" aria-expanded="false">
                                    <img src="https://github.com/mdo.png" alt="mdo" width="32" height="32"
                                        class="rounded-circle">
                                </a>
                                <ul class="dropdown-menu text-small">
                                    <li><a class="dropdown-item" href="#">New project...</a></li>
                                    <li><a class="dropdown-item" href="#">Settings</a></li>
                                    <li><a class="dropdown-item" href="#">Profile</a></li>
                                    <li>
                                        <hr class="dropdown-divider">
                                    </li>
                                    <li><a class="dropdown-item" href="#">Sign out</a></li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>


            </header>


            <div class="container-fluid mt-4">
                <div class="row">
                    <!-- Sidebar -->
                    <div class="col-lg-2 col-md-4 mb-4 position-sticky" style="top:80px;">
                        <div class="mb-4">
                            <label for="sort" class="form-label fw-bold">Sort by</label>
                            <select id="sort" class="form-select">
                                <option selected>Popular</option>
                                <option value="low-high">Price: Low to High</option>
                                <option value="high-low">Price: High to Low</option>
                            </select>
                        </div>

                        <div class="mb-4">
                            <h5 class="fw-bold">Collection</h5>
                            <ul class="list-unstyled">
                                <li><a href="#">New Collection</a></li>
                                <li><a href="#">All Products</a></li>
                                <li><a href="#">Discounted Products</a></li>
                            </ul>
                        </div>

                        <div>
                            <h5 class="fw-bold">Filter by</h5>
                            <ul class="list-unstyled">
                                <li><a href="#">Availability</a></li>
                                <li><a href="#">Discount</a></li>
                            </ul>
                        </div>
                    </div>



                    <!-- Cards section -->
                    <div class="col-lg-10 col-md-8">
                        <div class="px-4 py-3">
                            <h2 class="pb-2 border-bottom">Shop Now</h2>

                            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4 py-3">
                                <% products.forEach(function(product) { %>
                                    <div class="col">
                                        <div class="card h-100 overflow-hidden rounded-4 shadow-lg border-0">

                                            <!-- Top colored image/box -->
                                            <div style="background-color: <%= product.bgcolor %>; height: 250px;"
                                                class="w-100">
                                                <% if (product.image) { %>
                                                    <img src="data:image/png;base64,<%= product.image.toString('base64') %>"
                                                        alt="<%= product.name %>"
                                                        class="img-fluid w-100 h-100 object-fit-cover" />
                                                    <% } %>
                                            </div>

                                            <!-- Bottom panel -->
                                            <div class="d-flex justify-content-between align-items-center px-3 py-2"
                                                style="background-color: <%= product.panelcolor %>; color: <%= product.textcolor %>;">
                                                <div>
                                                    <div><strong>
                                                            <%= product.name %>
                                                        </strong></div>
                                                    <div>₹ <%= product.price %>
                                                    </div>
                                                </div>
                                                <a href="shop/addToCart/<%= product._id %>">
                                                    <button class="btn btn-light btn-sm rounded-circle">
                                                        +
                                                    </button>
                                                </a>
                                            </div>

                                        </div>
                                    </div>
                                    <% }) %>
                            </div>




                            <!-- Other cards here... -->

                        </div>
                    </div>
                </div>
            </div>
            </div>

            <footer class="bg-dark text-white pt-4 pb-3 mt-5">
                <div class="container">
                    <div class="row">

                        <!-- About -->
                        <div class="col-md-4 mb-3">
                            <h5 class="text-uppercase">Dog Store</h5>
                            <p class="small">Your one-stop shop for adorable and healthy dogs. Quality breeds. Trusted
                                care.
                                Happy tails!</p>
                        </div>

                        <!-- Quick Links -->
                        <div class="col-md-4 mb-3">
                            <h5 class="text-uppercase">Quick Links</h5>
                            <ul class="list-unstyled">
                                <li><a href="/shop" class="text-white-50 text-decoration-none">All Products</a></li>
                                <li><a href="/new" class="text-white-50 text-decoration-none">New Collection</a></li>
                                <li><a href="/discounts" class="text-white-50 text-decoration-none">Discounted Dogs</a>
                                </li>
                                <li><a href="/contact" class="text-white-50 text-decoration-none">Contact Us</a></li>
                            </ul>
                        </div>

                        <!-- Social -->
                        <div class="col-md-4 mb-3">
                            <h5 class="text-uppercase">Follow Us</h5>
                            <a href="#" class="text-white me-3"><i class="bi bi-facebook"></i></a>
                            <a href="#" class="text-white me-3"><i class="bi bi-instagram"></i></a>
                            <a href="#" class="text-white me-3"><i class="bi bi-twitter-x"></i></a>
                            <a href="#" class="text-white"><i class="bi bi-youtube"></i></a>
                        </div>
                    </div>

                    <div class="text-center text-white-50 pt-3 border-top mt-3">
                        <small>&copy; 2025 Dog Store. All rights reserved.</small>
                    </div>
                </div>
            </footer>

            <script>
                // Automatically hide flash message after 3 seconds
                setTimeout(() => {
                    const flash = document.getElementById('flash-msg');
                    if (flash) {
                        flash.style.transition = 'opacity 0.5s ease';
                        flash.style.opacity = '0';
                        setTimeout(() => flash.remove(), 500); // remove after fade out
                    }
                }, 1000);
            </script>




</body>