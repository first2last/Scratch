<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FurFrnd Shop</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #2AA8A8;
            --secondary: #FF6B6B;
            --dark: #2c3e50;
        }

        .brand-logo {
            font-size: 1.8rem;
            font-weight: 700;
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            padding: 0.5rem 0;
        }


        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(120deg, #ffffff, #e0f7f7);
            color: #2c3e50;
        }

        .navbar {
            background: rgba(255, 255, 255, 0.95);
        }




        .brand-logo {
            font-size: 1.8rem;
            font-weight: 700;
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            padding: 0.5rem 0;
        }

        .nav-link {
            color: var(--dark) !important;
            font-weight: 500;
            padding: 0.5rem 1rem !important;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .nav-link:hover {
            background: rgba(42, 168, 168, 0.1);
            color: var(--primary) !important;
        }

        .sidebar {
            background: rgba(255, 255, 255, 0.98);
            border-right: 1px solid rgba(42, 168, 168, 0.1);
            box-shadow: 2px 0 15px rgba(0, 0, 0, 0.05);
        }

        .sidebar-heading {
            color: var(--primary);
            font-weight: 600;
            border-bottom: 2px solid var(--primary);
            padding-bottom: 0.5rem;
        }

        .filter-link {
            color: var(--dark);
            transition: all 0.2s ease;
            position: relative;
        }

        .filter-link:hover {
            color: var(--secondary);
            padding-left: 1rem;
        }

        .filter-link:hover::before {
            content: '';
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 8px;
            height: 8px;
            background: var(--secondary);
            border-radius: 50%;
        }

        .shop-title {
            font-weight: bold;
            color: #2AA8A8;
        }

        .card {
            border: none;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.08);
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-10px);
        }

        .card img {
            height: 250px;
            object-fit: cover;
            width: 100%;
        }

        .card-img-container {
            height: 250px;
            overflow: hidden;
            position: relative;
        }

        .card-img-top {
            height: 100%;
            width: 100%;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        /* Remove fixed background from footer in CSS */
        .card-footer {
            /* Remove any existing background-color declaration */
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.75rem 1rem;
        }

        .btn-add-to-cart {
            border-radius: 50%;
            font-weight: bold;
            transition: all 0.3s ease;
        }

        .btn-add-to-cart {
            background: rgb(24, 2, 51);
            color: #2AA8A8;
            border: 2px solid #2AA8A8;
            border-radius: 50%;
            font-weight: bold;
        }

        .btn-add-to-cart:hover {
            background: #FF6B6B;
            color: white;
            border-color: #FF6B6B;
        }

        footer {
            background: linear-gradient(45deg, #2AA8A8, #FF6B6B);
            color: white;
            text-align: center;
            padding: 2rem 0;
        }

        .floating-cart {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background: #FF6B6B;
            color: white;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 1.5rem;
            z-index: 999;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .floating-cart:hover {
            background: #d94b4b;
        }

        .flash-message {
            background-color: #2AA8A8;
            color: white;
            padding: 1rem;
            border-radius: 8px;
            text-align: center;
            margin: 1rem 0;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-custom fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <span class="brand-logo">🐶 furfrnd</span>
            </a>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <% if (user) { %>
                        <li class="nav-item">
                            <a class="nav-link nav-link-custom" href="shop/cart">Cart</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link nav-link-custom dropdown-toggle" href="#" role="button"
                                data-bs-toggle="dropdown">
                                <i class="bi bi-person-circle me-1"></i>Account
                            </a>

                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><a class="dropdown-item" href="/profile">Profile</a></li>
                                <li><a class="dropdown-item" href="/orders">Orders</a></li>
                                
                                <li>
                                    <hr class="dropdown-divider">
                                </li>
                               
                            </ul>
                        </li>
                        <li><a class="dropdown-item text-danger" href="users/logout">Logout</a></li>
                        <% } else { %>
                            <li class="nav-item">
                                <a class="nav-link nav-link-custom" href="/">Home</a>
                            </li>
                            <li class="nav-item">
                                <a class="btn btn-outline-primary ms-2" href="/register">Login</a>
                            </li>
                            <li class="nav-item">
                                <a class="btn btn-primary ms-2" href="/register">Register</a>
                            </li>
                            <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <% if (success && success.length> 0) { %>
        <div class="container">
            <div class="flash-message">
                <%= success[0] %>
            </div>
        </div>
        <% } %>

            <a href="<%= user ? '/shop/cart' : '/register' %>" class="floating-cart">
                <i class="bi bi-cart"></i>
                <% const count=cartCount || 0; %>
                    <% if (count> 0) { %>
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                            <%= count %>
                        </span>
                        <% } %>
            </a>

            <div class="container-fluid mt-5">
                <div class="row">
                    <!-- Sidebar -->
                    <!-- Sidebar -->
                    <div class="col-lg-2 col-md-4 sidebar position-sticky">
                        <div class="p-4">
                            <h5 class="sidebar-heading mb-4">Filter & Sort</h5>

                            <div class="mb-4">
                                <label class="form-label fw-bold text-primary">Sort By</label>
                                <select class="form-select border-primary">
                                    <option>Popular</option>
                                    <option>Price: Low to High</option>
                                    <option>Price: High to Low</option>
                                </select>
                            </div>

                            <div class="mb-4">
                                <h6 class="sidebar-heading">Collections</h6>
                                <ul class="list-unstyled">
                                    <li><a href="#" class="filter-link py-2 d-block">New Arrivals</a></li>
                                    <li><a href="#" class="filter-link py-2 d-block">Premium Breeds</a></li>
                                    <li><a href="#" class="filter-link py-2 d-block">Family Friendly</a></li>
                                </ul>
                            </div>

                            <div class="mb-4">
                                <h6 class="sidebar-heading">Filters</h6>
                                <ul class="list-unstyled">
                                    <li><a href="#" class="filter-link py-2 d-block">Available Now</a></li>
                                    <li><a href="#" class="filter-link py-2 d-block">Special Offers</a></li>
                                    <li><a href="#" class="filter-link py-2 d-block">Puppy Training Kit</a></li>
                                </ul>
                            </div>
                        </div>
                    </div>


                    <!-- Products -->
                    <div class="col-md-9">
                        <h2 class="shop-title mb-4">Shop Now</h2>
                        <div class="row g-4">
                            <% products.forEach(function(product) { %>
                                <div class="col-md-4">
                                    <div class="card">
                                        <!-- Apply background color to image container -->
                                        <div class="card-img-container"
                                            style="background-color: <%= product.bgcolor %>;">
                                            <img src="data:image/png;base64,<%= product.image.toString('base64') %>"
                                                alt="<%= product.name %>" class="card-img-top">
                                        </div>

                                        <!-- Apply panel color and text color to footer -->
                                        <div class="card-footer" style="background-color: <%= product.panelcolor %>; 
                                                    color: <%= product.textcolor %>;">
                                            <div>
                                                <strong>
                                                    <%= product.name %>
                                                </strong><br>
                                                ₹ <%= product.price %>
                                                    <% if (product.discount> 0) { %>
                                                        <span class="text-danger ms-2">
                                                            <del>₹ <%= product.price + product.discount %></del>
                                                        </span>
                                                        <% } %>
                                            </div>
                                            <a href="/shop/addToCart/<%= product._id %>">
                                                <button class="btn btn-add-to-cart" style="color: <%= product.textcolor %>;
                                                               border-color: <%= product.textcolor %>;">
                                                    +
                                                </button>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                                <% }) %>
                        </div>
                    </div>
                </div>
            </div>

            <footer class="mt-5">
                <div class="container">
                    <p>&copy; <%= new Date().getFullYear() %> FurFrnd. All rights reserved.</p>
                </div>
            </footer>

            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>